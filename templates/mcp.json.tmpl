{
  "_comment": "MCP server configuration for scholar-flow. Blocks are conditionally included by /init based on user selections: SEMANTIC_SCHOLAR_BLOCK (academic paper search), ARXIV_BLOCK (preprint search), PAPER_SEARCH_BLOCK (multi-source aggregation), ZOTERO_BLOCK (reference management), OBSIDIAN_FS_BLOCK (note-taking vault access), SEQUENTIAL_THINKING_BLOCK (complex reasoning).",
  "mcpServers": {
    {{SEMANTIC_SCHOLAR_BLOCK}}
    {{ARXIV_BLOCK}}
    {{PAPER_SEARCH_BLOCK}}
    {{ZOTERO_BLOCK}}
    {{OBSIDIAN_FS_BLOCK}}
    {{SEQUENTIAL_THINKING_BLOCK}}
  }
}
