---
description: Full verified multi-stage research pipeline (orchestrator)
agent: build
---

Run a deep, verified research pipeline on the topic: $ARGUMENTS

Delegate to the `deep-dive` subagent via the `task` tool. The subagent orchestrates a multi-phase pipeline:
intent planning → literature search + coverage audit → citation verification → paper analysis with audit → ideation with novelty check → cross-synthesis → wisdom extraction.

If no topic is provided, ask the user for one before delegating.

Research field context: AI in Education (PhD level)
